public class solution {
       public static int arrayRotateCheck (int[] arr) {
            int size = arr.length;
            for(int i =0; i<size; i++) {
              if(arr[i]> arr[i+1]) {
                    return i+1;
              }   
            }
            return 0;
       }
}
